@* The purpose of this component is to simulate signed in user with a player ID *@

<CascadingValue Value="@playerId" Name="PlayedId">
    @ChildContent
</CascadingValue>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    private int playerId = 1;
}
